{
  "type": "module",
  "scripts": {
    "build": "npx tsx scripts/build.ts",
    "deploy": "npx tsx scripts/deploy.ts",
    "test": "vitest",
    "lint": "npx eslint ."
  },
  "devDependencies": {
    "@jill64/types-lambda": "1.1.1",
    "dotenv": "16.4.0",
    "vitest": "1.2.1"
  }
}
