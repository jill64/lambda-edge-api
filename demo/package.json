{
  "type": "module",
  "scripts": {
    "build": "npx tsx scripts/build.ts",
    "deploy": "npx tsx scripts/deploy.ts",
    "test": "vitest",
    "lint": "npx eslint ."
  },
  "devDependencies": {
    "@jill64/types-lambda": "1.2.3",
    "dotenv": "16.4.7",
    "vitest": "3.0.2"
  }
}
