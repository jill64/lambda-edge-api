{
  "type": "module",
  "scripts": {
    "build": "npx tsx scripts/build.ts",
    "deploy": "npx tsx scripts/deploy.ts",
    "test": "vitest --run"
  },
  "devDependencies": {
    "@jill64/types-lambda": "1.0.8",
    "dotenv": "16.3.1",
    "vitest": "0.34.6"
  }
}
